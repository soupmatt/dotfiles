[core]
  excludesfile = ~/.gitignore
  editor = vim
[branch]
  autosetuprebase = always
[fetch]
  prune = true
[github]
  user = soupmatt
[rebase]
  autosquash = true
  autoStash = true
  updateRefs = true
[init]
  defaultBranch = main
[push]
  autoSetupRemote = true

{{ if not .spin -}}
[filter "lfs"]
  smudge = git-lfs smudge -- %f
  process = git-lfs filter-process
  required = true
  clean = git-lfs clean -- %f
[user]
  name = {{ .name }}
  email = {{ .email }}
[include]
  path = /Users/matt.campbell/.config/dev/gitconfig
{{- end }}
